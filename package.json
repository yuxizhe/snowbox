{
  "private": true,
  "name": "snowbox-monorepo",
  "version": "0.1.0",
  "description": "Universal UI system",
  "main": "index.js",
  "scripts": {
    "dev": "cd packages/examples && yarn dev",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "echo \"build\"",
    "build:es5": "cd packages/snowbox && rm -rf dist && yarn tsc && cp -rf ./src/Icon/images ./dist/Icon/images"
  },
  "workspaces": [
    "packages/*"
  ],
  "keywords": [
    "react-native",
    "react-native-web"
  ],
  "author": "yuxz",
  "license": "MIT"
}
