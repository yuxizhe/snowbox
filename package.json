{
  "private": true,
  "name": "snowbox-monorepo",
  "version": "0.1.0",
  "description": "Universal UI system",
  "main": "index.js",
  "scripts": {
    "dev": "cd packages/examples && yarn dev",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "echo \"build\"",
    "packs": "cd packages/snowbox && npm pack",
    "build:es5": "cd packages/snowbox && rm -rf dist && yarn tsc && cp -rf ./static ./dist/static",
    "snowlink": "cd packages/snowbox && npm link"
  },
  "workspaces": [
    "packages/*"
  ],
  "keywords": [
    "react-native",
    "react-native-web"
  ],
  "author": "yuxz",
  "license": "MIT"
}
